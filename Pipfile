[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
djangorestframework = ">=3.9.0,<3.12.0"
Django = ">=2.1.3,<2.3.0"

[dev-packages]
black = "*"

[requires]
python_version = "3.7"

[scripts]
start = "docker-compose up -d"

[pipenv]
allow_prereleases = true
